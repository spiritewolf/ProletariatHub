/** Canonical client routes (single source for path literals). */
export enum AppPath {
  Root = '/',
  Docs = '/docs',
  Shopping = '/shopping',
  Login = '/login',
  ChangePassword = '/change-password',
  Setup = '/setup',
}
