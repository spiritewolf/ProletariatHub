/** Canonical client routes (single source for path literals). */
export enum AppPath {
  Root = '/',
  Shopping = '/shopping',
  Login = '/login',
  ChangePassword = '/change-password',
  Setup = '/setup',
}
